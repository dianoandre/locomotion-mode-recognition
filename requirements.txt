# Python 3.11 or 3.12 (some dependencies have no wheels for 3.13 yet).
# Install with: python -m pip install -r requirements.txt

numpy>=1.26,<3
pandas>=2.2,<3
scipy>=1.15
mat-io>=0.4.2,<1.0        # imported as `matio`; reads MATLAB table objects
pyarrow>=15.0             # Parquet backend for the cache
scikit-learn>=1.4
imbalanced-learn>=0.12
xgboost>=2.0              # 2.0+ needed for iteration_range in predict
shap>=0.45
statsmodels>=0.14
matplotlib>=3.8
seaborn>=0.13
tqdm>=4.66
joblib>=1.3
ipykernel>=6.29
jupyterlab>=4.0
